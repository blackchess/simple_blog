server:
  port: 8001

spring:
  application:
    name: eureka-server

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:8001/eureka
